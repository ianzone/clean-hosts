version: '1.0'
name: build
displayName: build
triggers:
  trigger: auto
  schedule:
    - cron: '* * 1 * * ? *'
stages:
  - name: build
    displayName: build
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: build_nodejs
        displayName: Nodejs 构建
        nodeVersion: 24.13.0
        commands:
          - '# 设置 NPM 源，提升安装速度'
          - npm config set registry https://registry.npmmirror.com
          - ''
          - '# 执行编译命令'
          - npm install -g bun
          - bun run build
        caches: []
        notify: []
        strategy:
          retry: '0'
